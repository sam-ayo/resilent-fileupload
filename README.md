# Goal

Make a resilent file upload in the browser that can handle browser crash e.t.c

Uses tus protocol + uppy to achieve file upload resumability + Golden Retrival uppy plugin to get files incase of browser crashes, refresh, or other ways files could be lost during file upload.
